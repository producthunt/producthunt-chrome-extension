/**
 * Dependencies.
 */

let keyMirror = require('react/lib/keyMirror');

/**
 * Constants.
 */

const consts = keyMirror({
  RECEIVE_DATA: null
});

/**
 * Export `consts`.
 */

module.exports = consts;
